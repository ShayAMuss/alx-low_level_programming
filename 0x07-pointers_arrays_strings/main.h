#ifndef _MAIN_H
#define _MAIN_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void print_diagsums(int *a, int size);
char *_memset(char *s, char b, unsigned int n);
int _putchar(char c);

#endif
