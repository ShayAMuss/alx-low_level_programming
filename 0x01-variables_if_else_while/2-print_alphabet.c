#include <stdio.h>
/**
 * main - Program entry point
 * putchar: Function to print alphabets
 *
 * Return: 0 on success.
 */
int main (void)
{
	char c = 'a';
	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
return (0);
}
